{% extends 'page.html.twig' %}
{% block title %}Annonce créée{% endblock %}
{% block body %}
    <div class="ui segment center aligned">
        <h2 class="ui center aligned icon header">
            <i class="horn icon"></i>
            Votre annonce a bien été créée.
        </h2>
        <div>
            <div class="ui large buttons">
                <a href="{{ path('connexion') }}" class="ui green button">Se connecter</a>
                <div class="or" data-text="ou"></div>
                <a href="{{ path('homepage') }}" class="ui button">Page d'accueil</a>
            </div>
        </div>
    </div>
{% endblock %}
{% extends 'page.html.twig' %}
{% block title %}FealJob - Mail vérifié{% endblock %}
{% block body %}
    <div style="padding-top: 20px;" class="ui middle aligned center aligned grid">
        <div class="column">
            <h2 class="ui center aligned icon header">
                <i class="envelope open outline icon"></i>
                Votre e-mail a bien été vérifié.
            </h2>
            <div>
                <div class="ui large buttons">
                    <a href="{{ path('connexion') }}" class="ui green button">Se connecter</a>
                    <div class="or" data-text="ou"></div>
                    <a href="{{ path('homepage') }}" class="ui button">Page d'accueil</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}